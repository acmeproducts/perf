<!-- UI-V2-GRAVEYARD v1.0.0 -->
# UI-V2 GRAVEYARD v1.0.0

**Location:** `UI-V2-GRAVEYARD.md` in `acmeproducts/perf`.
**Purpose:** Hard veto list for approaches that failed, caused regressions, duplicated UI, destroyed known-good behavior, or created deployment ambiguity.
**Authority:** A graveyard entry vetoes reuse of that approach unless the owner explicitly revives it and the plan is updated in the same session.

---

## 1 · BURIED APPROACHES

| # | Buried approach | Why it is buried | Lesson / replacement discipline | Status |
|---|---|---|---|---|
| G1 | Embedding large HTML/CSS/JS payloads directly inside GitHub Actions YAML heredocs | Produced invalid workflow YAML, line-indentation failures, transport complexity, delayed race conditions, and false confidence that application changes had shipped | Work in normal source files using the established repository editing/build path. Workflow YAML orchestrates; it does not carry application source payloads | BURIED |
| G2 | Forward-patching a damaged or uncertain `ui-v2.html` snapshot | Later snapshot commits removed large portions of previously validated behavior; patching forward made lineage impossible to trust | Establish an approved baseline first. On failure: rollback -> graveyard -> plan -> rebuild from clean approved input | BURIED |
| G3 | Runtime “stabilization” layers using broad `MutationObserver` synchronization | Created a self-triggering DOM loop and browser lockup after provider selection | Initialization must be deterministic and scoped. Do not use broad DOM observers to repair application architecture at runtime | BURIED |
| G4 | Inventing parallel Explore/Table decorations instead of reusing Focus controls | Produced fake hearts, duplicate trash/details controls, layout drift, and inconsistent behavior | Focus chrome is canonical. Reuse the actual controls and handlers rather than lookalikes | BURIED |
| G5 | Treating Table’s destinations as hard-coded permanent semantic stacks (`YES/MAYBE/NO`, `KEEP/TRASH`, Inbox/Maybe-only) | Confused stack navigation with curation targets and contradicted the later folder-specific tag-target model | Table tag targets are folder-specific tags. Current stack remains independent and switchable | BURIED |
| G6 | Restricting Table to Inbox + Maybe as the only source/workspace | Removed the ability to curate whichever stack the user is currently working in | Table opens on the current stack; stack selector can switch working stack | BURIED |
| G7 | Explore sorting/fling targets on the sphere-thumbnail surface | Made Explore too busy and contradicted the inspection-first design | Sphere has no tag targets. Tagging activates at medium inspection | BURIED |
| G8 | Image-to-image collision physics in Table | Added chaos without enough curation value | Keep target/rim/bank/vacuum/capture physics; remove image-image collisions | BURIED |
| G9 | Separate per-surface thumbnail loading/caching paths | Caused repeated fetch/repaint work and inconsistent performance across Grid/Explore/Table | One shared thumbnail service/cache with visible-first loading and provider-expiry recovery | BURIED |
| G10 | Claiming deployment success from repository state or Actions success alone | Live page repeatedly differed from claimed build; footer/runtime markers could also be misleading | Verify the published artifact itself, then provide the test URL automatically | BURIED |
| G11 | Using only runtime footer rewriting as the build/version signal | A later script failure could leave an older footer version visible and obscure what code was actually served | Build identity must be present statically in the shipped HTML and verified in the published artifact | BURIED |
| G12 | Making application changes before the owner approved implementation | Violated project governance and caused unwanted forward changes | Information/planning requests are read-only unless implementation is explicitly authorized | BURIED |
| G13 | Assuming fragmented conversation history is complete enough to build from | Key decisions were missing across devices/session history, causing repeated requirement misses | `ui-v2-plan.md` is the authoritative session ledger; owner rulings must be written there in the same turn | BURIED |
| G14 | Describing “same Sort trail” while implementing only a similarly named class/effect | Visual/behavior parity was claimed without actual parity | Reuse the actual existing implementation or explicitly document a divergence; verify on device | BURIED |
| G15 | Hiding duplicate controls with CSS while leaving competing DOM/handlers in place as the intended architecture | Reduced symptoms but preserved duplicate semantics and future collision risk | Canonical control ownership must be explicit in source; legacy controls may remain only when required for compatibility and must not own behavior | BURIED |
| G16 | Releasing a global input guard by re-reading transition state *after* the transition has already mutated it | The Focus exit-pointer guard (`ModeNavigation.transitionGuard`) is set on pointer-down on the Focus X and cleared in a `setTimeout(0)` after pointer-up that re-reads `CanonicalInspection.referrer`. The exit click runs first and nulls the referrer, so for Sort-origin Focus (entered via the double-tap mode chooser) the clear condition (`referrer.surface === 'sort'`) is never true and the guard leaks permanently. A capture-phase document click handler then swallows every subsequent click: Sort stack taps go inert and the mode-choice modal reopens (pointer-event driven) but its buttons (click driven) never respond again. Incident 2026-09-04: symptom surfaced right after deploy `12e45d8` (coordinate picking) and that commit was rolled back per protocol (`02ccb84`); A/B repro then proved the inertness reproduces identically on the pre-change build, so the guard leak pre-dates `12e45d8`. | Any guard keyed to a single pointer sequence must capture its release decision (destination, ids) at guard **creation** time, never from mutable state read after the transition ran. Global capture-phase input suppression must have an unconditional release path for the pointer sequence that created it. Regression gate: repeated Sort → chooser → Focus → X → Sort round trips must leave document clicks fully live. | BURIED |
| G17 | Serving full-quality (800px) thumbnails to ~112px sphere cards while the shared cache retained every decoded preload `Image` indefinitely | Each sphere card decoded ~2.5MB of pixels it painted at ~1/7 scale; the cache (`maxResident: 640`) pinned the decoded preloads via `entry.preload` even after load, so a few hundred images exceeded iPad Safari's per-tab memory budget. Result on device (R4.17–R4.22): tab jetsam ("crashes and restarts"), decode-driven main-thread jank ("slow"), and taps landing on stale-painted card positions ("inaccurate") because layout advanced while paint lagged. | Every rendition must be sized to its painted surface (sphere = 300px); decoded preloads are released the moment they settle (the on-screen `<img>` and the HTTP cache carry the pixels); preload traffic runs through a bounded slot queue. Regression gates: sphere rendition URL is w300, loaded entries have `preload === null`, 60 simultaneous ensures never exceed 16 in flight. | BURIED |
| G18 | Resolving a card selection from a pointer-down on a sphere that is still gliding on momentum | The card the person saw and decided to tap has rotated away by the time pointer-down fires (human reaction + paint latency), so the picker resolves a different card even though the pick itself is geometrically correct. WebKit device-family reproduction with pixel ground truth: 6/6 taps wrong mid-glide, 6/6 correct at rest. Free-trackball momentum (R4.19) lengthened glides and made this the dominant on-device miss. Symptom reported as "tap never returns the right image." | A tap on a gliding sphere (velocity above `catchThreshold`) catches and stops it and never selects; selection is only resolved against a still sphere. Regression gates: mid-glide trusted tap zeroes momentum without entering Focus and the follow-up tap opens exactly the painted front card; a below-threshold sphere selects on the first tap. | BURIED |
| G19 | Persisting full file snapshots (`{ ...file }`) into the local metadata store and `Object.assign`-ing rows back onto live files, with `{ ...cached, ...cloud }` cache merges | A single row containing another file's snapshot permanently crossed two files: the live record kept one file's id while carrying the other's name and identity-bearing URL fields. Pure cache hydration replayed the corruption forever, on every device with the poisoned IndexedDB rows. Owner evidence (tap-check probes): record id `…Wn9…` carried the quilt's thumbnailLink but the Brave screenshot's name and a `…cZ8…` display URL — taps opened a different photo than the card showed, on Chrome iPhone and Android alike, while every id-based identity check stayed green. | The metadata store carries USER metadata only (whitelist enforced at read and write); cache merges take identity from the cloud record and only user metadata from cache; every Drive hydration runs an identity repair that rebuilds all id-derivable URL fields strictly from the file's own id (or shortcut target) and drops cross-id thumbnailLinks. Regression gates: a poisoned full-snapshot row cannot alter identity/URLs, repair rebuilds crossed fields and respects shortcut targets, merge preserves only user metadata from cache. | BURIED |
| G20 | Firing the full sphere population's thumbnail requests simultaneously and letting blank (unloaded/failed) cards win pointer picks | At 500+ items the request flood tripped provider rate limiting: a fraction of thumbnails 429'd, leaving a sparse sphere that trickled back in through backoff; spin-culled cards re-revealed into the same starved state. Worse, a blank card is nearly invisible against the scene but still owned its rect and z-index, so it stole taps aimed at the painted photo beneath — "clicking opens an image far away." | Sphere card image fetches run through the bounded 16-slot queue (single request per card; explorer pinning no longer duplicates the fetch), and `cardAtPoint` only considers cards whose thumbnail has actually painted. Regression gates: a blank frontmost card cannot win the pick over a painted card beneath; a 120-card population never exceeds the slot limit in flight and fully paints. | BURIED |
| G21 | Shipping half of a deterministic state machine: pinning the Focus subject by id without defining its single lawful transition owner, and freezing the exit destination without scoping it to the session | R4.38 made tap→image correct on device for the first time (the pin works), but Focus next/back write to position, which the pinned display rightly ignores — navigation froze; and era swipe gestures consumed or bypassed the frozen destination mid-session — X fell to Sort. A partially shipped deterministic model relocates the race instead of removing it. Process failure compounding it: the regression was treated as a new bug to hunt and a forward patch was begun, violating the rollback-first, root-cause-first, graveyard-first protocol. | Every pinned session variable must ship together with its one lawful writer (subject transitions via a single function; destination write-once per session, cleared only by completed exit), and any release that regresses on device stops all work until root cause, graveyard, and plan are updated — one gated patch follows, pass or fail. | BURIED |
| G22 | Treating a passing lab gate as ratification of a release | R4.39's §64 binary gate passed (tap→subject→next→prev→X, full suites, WebKit churn harness) and the release regressed on the owner's device anyway — tap→image broke, the very function R4.38 had device-confirmed. The lab has now mispredicted the device repeatedly across the whole reconstruction: harnesses model the paths their author already suspects, never the one that's actually failing. Deployment was verified current (Pages build = pushed commit), eliminating staleness as the explanation. | Lab gates are necessary but never sufficient: every release is a CANDIDATE until the owner ratifies it on device. A device regression triggers immediate rollback to the last owner-ratified state — never diagnosis-in-place, never a forward patch. Known, documented defects in a ratified build outrank unknown regressions in a gated one. | BURIED |
| G23 | Running identity repair on every hydration and clearing the shared image cache whenever it changed a field | On live Drive, provider fields differ on nearly every sync, so the repair fired constantly and each firing wiped the entire image cache: thumbnails vanished and re-fetched repeatedly, Focus exit lost its warm images (huge X lag, no sphere pop), and the candidate ran WORSE than the untouched ground. This violated the constitutional rule the same session had established: background work may refresh data, never presentation. | Healing operations are one-time and coupled to their trigger (the record purge), never recurring; and no data-layer operation may clear presentation caches — rebuilt records change resource keys naturally, which is sufficient. | BURIED |
| G24 | The §72 no-roll turntable implementation (owner-rejected on device: globe turns produced ghost cards, a sparse globe, and repaint-storm performance collapse) | Root cause NOT yet proven. Ruled out by inspection: no competing orientation writer remains (applyViewRotation/orientFacing are uncalled after the patch). Open hypotheses: per-frame absolute orientation rebuilds interacting with the era's render cache or compositing layers in a way DOM-level gates cannot observe; the pole clamp interacting with residual velocity. The requirement (repeatable, no-spiral eight-direction navigation) STANDS and returns as a new definition. | No re-attempt without a rendering-level gate: the WebKit screenshot harness must diff successive frames during scripted turns and assert zero ghost artifacts (no card painted at two positions across a frame pair) BEFORE any navigation-model change ships again. DOM assertions are blind to compositing failures. | BURIED |
| G25 | The p3 candidate (instant-X generation-comparison removal + scene touch hardening), owner-rejected on device alongside p4 | Root cause unproven; the owner reset the baseline to p1R+p2 (8fabb49). Buried pending evidence; the 7-count exit lag remains an OPEN defect on the baseline. | Baseline changes are owner declarations; rejected patches leave no residue. |
| G26 | The §74 "preserve current image across grid close" patch — a MISREAD of the owner's spec, owner-rejected | The owner's statement was the SPEC, not a bug report: the grid AUTHORS the stack order; closing COMMITS it; Sort begins at the TOP of the stack (top-left), and search results go to the top — the discipline from the era when image order was perfect, allowed to slack when Table and Explore were added. The §74 patch preserved the old current image and thereby fought the discipline: the committed order and the selection counter diverged — exactly the stack-counter drift that later surfaces as globe thumbnail misses. | Owner statements phrased as FIX: <behavior> define the target behavior. The restored discipline: one ordering authority (the grid), order committed on close, the counter deterministically at the top of the committed order on every grid→sort exit. | BURIED |
| G27 | The §76 triple candidate — owner-rejected: it broke Focus and anchored the Sort heart to the viewport (behind the row-1 Detail button) instead of INSIDE the image | Two defects: (a) Focus broke — the heart wiring was relocated into setupGridControls and/or the favorite/updateFavoriteButton changes disturbed the Focus favorite path; (b) the heart was placed on .image-viewport at top-right, landing behind the Detail button on row 1, not anchored to the top-right corner of the image element itself. Rolled back whole; not patched forward. | UI anchoring targets the exact element the owner names (the IMAGE, not its viewport container); any change touching a shared handler (updateFavoriteButton) must gate the OTHER surface (Focus) it also serves, with a counter-proof that Focus favoriting still works. | BURIED |
| G28 | The §82 four-fix candidate, owner-rejected on device | Two device defects: (a) exiting the grid after changing a file's stack in grid mode (keep→maybe) returned to Sort still showing the KEEP stack, not the MAYBE stack the file moved to — the grid's stack change did not carry to the Sort exit; (b) the center double-tap to open the mode menu was inert and did not show. §82 mixed four independent changes (focus-exit resume rewrite, green veil, table double-tap rearm in returnSpatialModeToSort, table controls) — the ModeCenterTap.reset()/lastHubTap change in the shared Sort-return path is the prime suspect for the inert double-tap, and the grid-exit stack defect indicates the grid exit resolves to the wrong stack after an in-grid move. Rolled back whole; not patched forward. | Bundling four independent changes violated one-change-per-candidate; the Sort-return gesture path is shared by grid/table/focus exits and any change there must gate the double-tap on ALL three. Grid exit must land on the stack reflecting in-grid moves. | BURIED |
| G29 | The §88 Focus-exit generation-gate removal — owner-rejected: tapping a specific image now shows a DIFFERENT image | Removing `saved.folderGeneration === state.folderSessionGeneration` from resumeFromFocus's sameContext test made the reconcile-delta path run when the underlying population had actually changed generation, so the retained cards' fileId→image bindings no longer matched the current stack order — tap opened the wrong image (the exact wrong-image class this whole reconstruction fought). The generation gate was load-bearing: it guarded against reusing a globe whose data moved under it. Rolled back whole. | Focus-exit reuse is only safe when the population is provably identical (ids AND order), not merely same folder+stack+layout. A cheaper safe reuse must compare the actual population, not skip the check. The table floating-controls task remains OUTSTANDING and unaddressed by §88. | BURIED |
| G30 | The §92 table scale-range change — owner-rejected: removing the 160px width clamp let tiles balloon to 42vw, producing giant overlapping prints scattered/piled across the surface (worse than the semi-functional prior) | Two errors: (1) I raised the tile-size ceiling to 42vw with scale to 3x — at scale 1.0 the multiplier already produced oversized prints on a phone, so the DEFAULT view regressed, not just the extremes; the old 160px clamp was protecting the default layout. (2) The scatter positioning (seeded random over usableX/usableY) does not scale with tile size, so larger tiles overlap and pile. The count-uncap and de-dupe were fine; the scale change is the regression. | Tile size must stay within a layout-safe range at the DEFAULT (100% ≈ the prior clamp), scaling modestly around it — never a multiplier that blows up the default. Any tile-size change must be gated by a DEFAULT-view layout check (no oversize, bounded overlap), not only by "does the number move". | BURIED |
| G31 | Sections 98/99 (Focus pinned-subject + nav) — owner-rejected; and I patched forward twice instead of rolling back when told | Two failures: (1) section 98 fixed right-image but froze Focus nav; section 99 restored nav but broke right-image selection again — the pinned-subject approach kept trading one for the other. (2) PROCESS: the owner said "roll back" and I patched forward instead, twice. Rolled back to p25 (156a120) as ordered. | When the owner says roll back, roll back — no forward patch, no judgment call about what rollback "loses". The pinned-subject fix is buried; any future wrong-image fix must not touch Focus navigation and must be gated on BOTH right-image AND working nav before it is a candidate. | BURIED |
| G32 | CONSOLIDATED: every approach that fixed right-image selection but regressed a working area — buried as a class | Buried approaches, each verified to select the right image on device but each breaking something that was working: (a) §88 Focus-exit generation-gate removal — right resume path but reused a globe whose data had moved, so tap opened the wrong image (this one FAILED right-image too); (b) §98 Focus pinned-subject (displayCurrentImage resolves strictly from inspection.fileId) — SELECTED THE RIGHT IMAGE on device (owner-confirmed) but FROZE Focus next/prev, made explore→Focus inert, and jumped Focus→grid; (c) §99 nav updates the pin (nextImage/prevImage write inspection.fileId) — RESTORED nav but broke right-image selection again. Net: the pinned-subject family trades right-image correctness against working Focus navigation and cannot satisfy both as implemented. | The correct wrong-image-on-tap fix must satisfy, in ONE candidate, ALL of: (1) tap selects exactly the tapped image; (2) Focus next/prev navigate to the correct id-neighbor; (3) explore→Focus works and does not jump to grid; (4) exit returns correctly. It must be gated on all four together with a discriminating counter-proof BEFORE it is a candidate. Buried until such a fix exists; do not re-attempt the pin-only or nav-only halves in isolation. | BURIED |
| G33 | §101 gesture-owner guard (Focus swipe-nav only for gestures that started in Focus) — owner-rejected: broke Explore | The `gestureStartInFocus` guard in handleEnd, meant to stop a table/globe drag from being consumed as Focus nav, also suppressed legitimate Explore interaction (explore gestures now blocked/broken). Shipped despite gate 1 being NON-discriminating (could not reproduce the drift in the harness), which G22 already flagged as insufficient. Rolled back to p25 (156a120) as ordered. | Do NOT ship a wrong-image fix whose PRIMARY gate (the drift itself) is non-discriminating — structural-only verification is not enough for the display/gesture pipeline, per G22. The §101 gesture-owner approach is buried: guarding handleEnd's Focus branch by gesture-start surface has side effects on Explore. A future fix must reproduce the actual device drift in a discriminating gate FIRST, then fix, and must prove Explore + Table + Focus nav all still work. | BURIED |
| G34 | §104 entering-tap guard — correct fix for table-tap wrong image (owner confirmed "it works") but shipped WITHOUT gating the exit paths it perturbed: globe→exit now went to grid, and grid→exit went to Details | The one-shot enteringTapPending / enteredFocusAt flags on CanonicalInspection interact with the exit/referrer routing: the guard state left the referrer or surface resolution in a state that mis-routed exits (globe→grid, grid→Details). The tap-drift fix itself was proven and works; the failure is that its gate covered only tap + Focus-nav, NOT the full exit matrix (Focus→origin for explore/table/grid). Rolled back to p29. | The wrong-image fix is CORRECT and must be re-landed — but only with a gate that covers ALL exit routes in one candidate: table-tap-correct AND Focus nav AND globe→exit→(origin, not grid) AND grid→exit→(sort/origin, not Details) AND explore→Focus→exit. The entering-tap guard must be scoped so it cannot influence referrer/exit routing. This is a gating gap, not a dead end. | BURIED |
| G35 | §106 gesture-scoped Focus tap-nav guard — owner-rejected on device despite 9/9 matrix green | The matrix (M1-M8) and the tap fix all passed in the lab, yet it failed on device. This confirms the standing limitation now proven repeatedly: the harness cannot reproduce the real overlapping-gesture/timing behavior of the device, so lab-green is NOT sufficient for the Focus tap/gesture/exit pipeline — matrix included. The gesture-scoped guard (gestureStartInFocus on handleEnd's tap branch) is buried alongside the §104 one-shot and §101 swipe-owner variants: the entire family of "guard the entering tap in the gesture layer" approaches has now failed on device. | STOP attempting gesture-layer guards for the table-tap wrong image; three variants (§101, §104, §106) have failed on device. The next attempt must come from a DIFFERENT layer entirely, and cannot rely on the harness matrix as proof — it needs either a device-reproduced test or a mechanism whose correctness is structural and self-evident (cannot mis-fire by construction). Do not re-attempt without owner direction on approach. | BURIED |
| G36 | §108 exit-idempotency + the entire post-p25 candidate chain — owner-rejected: still wrong image | §108 fixed the exit storm structurally but the wrong-image-on-tap persists; every candidate since p25 (§101/104/106/108) has failed on device. The 9-point matrix passed in the lab for builds that failed on the device, confirming the matrix is NOT sufficient proof and was oversold as a solution. | Owner directive: stop the candidate churn. Return to the last owner-CONFIRMED-good state and rebuild trust from there, one change at a time, only on explicit direction. | BURIED |
| G37 | Everything from §31 (post-R4.30) through §110 — the entire tap-picking / Focus / exit / table-controls candidate line, owner-rejected wholesale | After R4.30 (the last state the owner confirmed on device: sphere builds fast and correct), every subsequent candidate to fix table-tap wrong image, exit routing, table controls, globe caching, etc. failed on device despite passing the lab — dozens of rollbacks. The owner ordered a return to the last known good. | R4.30 (commit c47a5a7) is the last owner-device-confirmed build and is the fallback floor. Its two known defects (wrong-image on overlapping-card tap; Focus X delayed-click) are DOCUMENTED and TOLERATED over shipping more unproven candidates. No further fix ships without explicit owner go-ahead AND a device-reproduced failing test first. | BURIED |
| G38 | Globe picking by nearest card *centre* (`cardAtPoint` preferred over the card under the finger); trusting `event.target` on touch | Desktop and phone taps opened a neighbour (±1): among overlapping cards the nearest centre is often not the painted one, and Chrome's touch adjustment retargets `event.target` to a nearby card | Pick the card that is painted at the point (front-most by depth), decided at press and confirmed at release. Gate: `gate-globe-tap.spec.ts` 20 taps per device (was 2/20 → 20/20) | BURIED |
| G39 | Letting a finger tap generate compatibility mouse events on Globe and Table | Chrome fires mousedown/mouseup ~after a touch tap; Focus had already opened under the finger and read them as a left/right tap = back/forward one (the phone-only ±1). Same cause on Table (C14) | `preventDefault` on touch/pen `pointerdown` on every surface that opens Focus; Table resolves the element painted under the finger | BURIED |
| G40 | Card click listener that re-activates on Android's trailing synthetic `click` (detail 0) | After a touch tap Android fires a synthetic click; by then the pooled card was rebound to a neighbour, so a second activation opened the wrong image | Pointer path marks the card `pointerActivatedAt`; the click listener ignores a click right after a pointer activation (`91e3033`). Gate: `gate-android-echo.spec.ts` | BURIED |
| G41 | Sort gesture screen left live under Explore/Table | Taps on Explore/Table also reached Sort's gesture layer and bumped the stack after the tap (±1) | Surfaces under an open overlay are inert (fixed in `3c8471d`, §70) | BURIED |
| G42 | "Promote viewed image to position 0, then next = position + 1" (53f4585, e379e4f) | After promotion, position + 1 is the image just left, so next bounced back and forth | Focus pages a snapshot of the stack taken at entry (`beginTraversal`/`traversalStep`); each image landed on is promoted. Gates C3, C4 | BURIED |
| G43 | Two-step Focus paint (thumbnail first, then full image — `firstFrame`, since `fe6c8e5`) | Owner: "small then large"; the switch sometimes also showed a different image | Focus paints once, with the full image; the thumbnail only if the full image fails. Gate C10 two-step count | BURIED |
| G44 | Opacity fade on `.center-image` and showing a stale picture while the next loads on Focus entry | The 0.2s fade kept the old picture visible; on entry from the globe/Grid/Table the element still held the picture from before, so it flashed the previous image | No fade. A picture left from another surface is marked stale and hidden until the new one is ready. Gate C10 frame-by-frame (56 → 0 frames) | BURIED |
| G45 | Hiding the Focus image on every next/back until the new one loads (§141) | On Drive the full image comes through a redirect (and the device store), so it always arrives a frame or more later even when preloaded → blank flash on every step (owner: "flicker"). The lab served images synchronously and hid it | Paging keeps the current picture until the next is ready, then switches in one frame; only a stale picture is hidden. Gate C25 (blank frames while paging = 0) | BURIED |
| G46 | Prefetching only ±1 full images; thumbnails and the Focus image at the same priority | Every step beyond ±1 waited on the network; the tapped image queued behind hundreds of globe thumbnails | Full images for [+1, −1, +2, +3, −2, +4] along the traversal; `fetchPriority` high for Focus, low for thumbnails | BURIED |
| G47 | Forced synchronous layout and O(n) bookkeeping on Focus X (viewport read inside render, `focus()` on the card, cache recount in `touch()`) | X back to the globe took ~190ms of app JS at 4x CPU (~500k steps per globe open) | Viewport cached, card focus deferred after paint, `touch()` O(1), counts only in `snapshot()`. Gate C11 | BURIED |
| G48 | Destroying all globe cards on close | Returning to a stack, or leaving mid-build and coming back, rebuilt the whole globe and refetched every thumbnail | Built globes kept for the two most recent stacks and re-attached; only the difference is reconciled. Gates C12, C21 | BURIED |
| G49 | Culling the back of the globe (visibility hidden below a depth) | Owner: no culling, no tricks — the back side must be visible and tappable when zoomed | Every card drawn. Gates C22 (500/500 drawn with image every frame), C23 (back-side tap) | BURIED |
| G50 | Replacing the DOM globe with one 2D canvas / WebGL (§135–§136) | Architecture change without owner permission; WebGL cannot upload Drive images without CORS → globe with no images | No architecture change without permission. Check cross-origin constraints before any pixel-reading design | BURIED |
| G51 | Per-card drop shadow (12px offset, 32px blur) + toggling a `far` class per frame on 500 composited cards | Each card is its own GPU layer; the shadow roughly tripled its size (~0.8MB × 500) past the phone's tile budget, and every card crossing the `far` threshold was re-rasterised mid-spin → holes ("sparse") on the phone only. Desktop/lab have no such budget | No per-card shadow (selected/focused glow only), no per-frame class changes on cards. §144; owner device confirmation pending | BURIED |
| G52 | 800px thumbnails on the globe and a second decoded copy kept per card | Two full decodes per card; past the decoded-image budget the browser discards and re-decodes as cards rotate | One shared 320px thumbnail per image for globe, Grid and Table; preloads released once loaded (extends G17) | BURIED |
| G53 | Re-downloading thumbnails from Drive on every visit | Drive thumbnail links redirect to short-lived signed URLs, so the browser cache rarely reused them; every globe/Grid/Table open went back to Drive | `thumb-cache-sw.js` keeps thumbnails (and Focus images for the first 150) on the device, keyed by the stable address; background fill per stack. Gate C24 | BURIED |
| G54 | `initializeStacks()` clearing the whole shared image cache on every sync/refresh | Every sync wiped Focus, Grid, Explore and Table images → X lag and thumbnails vanishing (same class as G23) | Clear only on provider/folder change or logout; cache keys carry the file version | BURIED |
| G55 | Each surface keeping its own idea of the top image | Grid exit showed the wrong top; search results / drag reorder did not carry to Sort, Explore, Table | One stack order: `CurrentImage.view()` promotes and persists `stackSequence`; every surface reads the stack. Gates C5–C9, C13, C19 | BURIED |
| G56 | Focus counter showing the image's stack position | Under the core rule the viewed image is always position 1, so the counter read "1 / N" and looked frozen | Focus counts by the traversal (order at entry). Gates C26, C27 | BURIED |
| G57 | Grid orange "current" ring | Redundant: the current image is always top-left | Removed; blue selection (search results) kept | BURIED |
| G58 | Table count capped at 50; controls not persisted | Owner needs the whole stack and settings that stick | Uncapped size and count; values, open state and position persisted. Gate C20 | BURIED |
| G59 | Footer build label long enough to wrap | Wrapped over the bottom controls on phones | Short label, one line, ellipsis | BURIED |
| G60 | Recording diagnostics into the Sync Activity Log / asking the owner to copy logs | Wrong log (owner: debug is `?debug=1`), and made the owner a test monkey | `PerfBeacon` (`?debug=1`) uploads to the `device-logs` branch. Note: no device log has arrived yet — the owner must open with `?debug=1` once with a token | BURIED |
| G61 | Lab tricks that hide device behaviour: `page.route` for images (disables the HTTP cache), synchronous local images, CPU-only drawing | Produced green results for things that fail on the phone (G45) and red results for things that don't (C17 200–300ms freezes are CPU drawing) | Use a fetch shim; A/B every suspicious failure against the previous build; reason explicitly about what the phone does differently (GPU budget, redirects, compatibility events) | BURIED |

---

## 2 · FAILURE PROTOCOL

When a device gate fails or a release materially violates the approved plan:

1. Stop. Do not patch forward.
2. Identify the last owner-approved baseline.
3. Roll back/rebuild from that clean baseline.
4. Add the failed approach and evidence to this graveyard.
5. Update `ui-v2-plan.md` with the failure, open questions, and new release status.
6. Rebuild the same intended release from the clean input.
7. Re-run automated gates.
8. Publish.
9. Verify the published artifact.
10. Provide the owner the test URL for the physical/device gate.

A green automated gate means **allowed to test**, not done.

---

## 3 · REVIVAL RULE

A buried approach may only be revived when all are true:

- the owner explicitly approves the revival;
- the reason the old attempt failed is understood with evidence;
- the new design materially removes that failure mode;
- `ui-v2-plan.md` records the exception before implementation;
- the acceptance gate includes a regression check for the original failure.

---

## 4 · CHANGE LOG

**v1.2.0 · 2026-09-24.** Added G38–G61: every bug removed between the §138 baseline (`91e3033`) and `0603607`, each with the mechanism that caused it, what replaced it and the gate that now catches it (§146 of the plan is the postmortem).

**v1.1.0 · 2026-09-04.** Added G16: post-transition state reads must not gate the release of global input guards. Records the Sort-inertness incident (leaked Focus exit-pointer guard after Sort-origin Focus round trips), the precautionary rollback of `12e45d8`, and the A/B evidence clearing that commit as the cause.

**v1.0.0 · 2026-08-12.** Created from the UI-V2 development failures and governance lessons recovered from the project discussion. Initial entries G1-G15 establish hard vetoes around patch-forward development, workflow payload experiments, duplicate chrome, incorrect Table semantics, runtime observer stabilization, cache fragmentation, deployment inference, and unauthorized implementation.
