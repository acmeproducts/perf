--- /tmp/true_baseline.html	2026-09-14 01:53:18.008832232 +0000
+++ /tmp/item3_only.html	2026-09-14 20:16:05.066062863 +0000
@@ -2767,12 +2767,17 @@
                 const token = ++this.presentationToken;
                 const fileId = String(file.id);
                 const displayEntry = this.resolve(file, 'display');
-                const thumbEntry = this.resolve(file, 'thumb');
                 const immediateEntry = immediateResource?.key ? this.entries.get(immediateResource.key) : null;
+                // Item 3 (plan section123.3): previously painted the cached 'thumb' rendition
+                // immediately, then swapped to 'display' a moment later -- a visible small-
+                // then-large step. Now only ever paints 'display'. The one fast path kept:
+                // if 'display' itself is already cached (revisiting an image), show it
+                // instantly, same as before for that case -- this was never the two-stage
+                // behavior the owner asked to remove, only the thumb-first interim paint was.
                 const firstEntry = immediateEntry && String(immediateResource.fileId) === fileId
                     && String(immediateEntry.fileId) === fileId && immediateResource.key === immediateEntry.key
-                    && immediateResource.loadedKey === immediateEntry.key ? immediateEntry
-                    : (thumbEntry?.readyState === 'loaded' ? thumbEntry : null);
+                    && immediateResource.loadedKey === immediateEntry.key && immediateEntry.rendition === 'display' ? immediateEntry
+                    : (displayEntry?.readyState === 'loaded' ? displayEntry : null);
                 const firstFrame = firstEntry?.src || '';
                 const binding = { key: firstEntry?.key || displayEntry?.key || '', fileId, surface, movement: false, loadedKey: firstEntry?.key || null, token };
                 const isCurrent = (entry, expectedKey = binding.key) => token === this.presentationToken
@@ -2791,53 +2796,35 @@
                     this.instrumentation.providerRequestCount++;
                 }
                 img.style.opacity = firstFrame ? '1' : '0';
+                let recovered = false;
+                img.onerror = async () => {
+                    const activeEntry = this.entries.get(binding.key);
+                    if (recovered || !isCurrent(activeEntry)) return;
+                    recovered = true;
+                    const fresh = await this.refreshFile(fileId);
+                    if (fresh && token === this.presentationToken && String(state.currentFileId) === fileId) this.present(img, fresh, { surface });
+                };
                 if (!firstFrame) {
-                    // No cached frame: show the thumb the moment it loads instead of holding the
-                    // screen blank until the full display rendition finishes downloading.
-                    const initialKey = binding.key;
-                    this.ensure(file, 'thumb').then(entry => {
+                    this.ensure(file, 'display', { surface }).then(entry => {
                         if (!entry?.src || entry.readyState !== 'loaded') return;
-                        if (token !== this.presentationToken || String(state.currentFileId) !== fileId) return;
-                        const activeBinding = this.bindings.get(img);
-                        if (activeBinding !== binding || binding.key !== initialKey || binding.loadedKey) return;
-                        if (String(entry.fileId) !== fileId) return;
+                        const priorKey = binding.key;
+                        const priorEntry = this.entries.get(priorKey);
+                        if (!isCurrent(priorEntry, priorKey) || String(entry.fileId) !== fileId || entry.key !== displayEntry?.key) return;
                         binding.key = entry.key;
-                        binding.loadedKey = entry.key;
+                        binding.loadedKey = null;
                         img.dataset.sharedResourceKey = entry.key;
+                        const commitDisplay = () => {
+                            if (!isCurrent(entry, entry.key)) return;
+                            binding.loadedKey = entry.key;
+                            img.style.opacity = '1';
+                        };
+                        img.addEventListener('load', commitDisplay, { once: true });
                         if (img.getAttribute('src') !== entry.src) {
                             img.src = entry.src;
                             this.instrumentation.providerRequestCount++;
-                        }
-                        img.style.opacity = '1';
+                        } else if (img.complete && img.naturalWidth > 0) commitDisplay();
                     });
                 }
-                let recovered = false;
-                img.onerror = async () => {
-                    const activeEntry = this.entries.get(binding.key);
-                    if (recovered || !isCurrent(activeEntry)) return;
-                    recovered = true;
-                    const fresh = await this.refreshFile(fileId);
-                    if (fresh && token === this.presentationToken && String(state.currentFileId) === fileId) this.present(img, fresh, { surface });
-                };
-                this.ensure(file, 'display').then(entry => {
-                    if (!entry?.src || entry.readyState !== 'loaded') return;
-                    const priorKey = binding.key;
-                    const priorEntry = this.entries.get(priorKey);
-                    if (!isCurrent(priorEntry, priorKey) || String(entry.fileId) !== fileId || entry.key !== displayEntry?.key) return;
-                    binding.key = entry.key;
-                    binding.loadedKey = null;
-                    img.dataset.sharedResourceKey = entry.key;
-                    const commitDisplay = () => {
-                        if (!isCurrent(entry, entry.key)) return;
-                        binding.loadedKey = entry.key;
-                        img.style.opacity = '1';
-                    };
-                    img.addEventListener('load', commitDisplay, { once: true });
-                    if (img.getAttribute('src') !== entry.src) {
-                        img.src = entry.src;
-                        this.instrumentation.providerRequestCount++;
-                    } else if (img.complete && img.naturalWidth > 0) commitDisplay();
-                });
                 return { token, fileId, firstFrame, displayEntry };
             },
             markMovement(img, active) {
